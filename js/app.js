        $(document).ready(function () {
            
            $("[id^=toggle]").hide();
            $(".82Check").change(function () {
                $("[id=toggle82]").toggle();
            });

        $('#selectall82').click(function(event) {  //on click 
            if(this.checked) { // check select status
                $('.checkbox82').each(function() { //loop through each checkbox
                    this.checked = true;  //select all checkboxes with class "checkbox82"               
                });
            }else{
                $('.checkbox82').each(function() { //loop through each checkbox
                    this.checked = false; //deselect all checkboxes with class "checkbox82"                       
                });         
            }
        });
    
            $("[id^=toggle]").hide();
            $(".85Check").change(function () {
                $("[id=toggle85]").toggle();
            });

        $('#selectall85').click(function(event) {  //on click 
            if(this.checked) { // check select status
                $('.checkbox85').each(function() { //loop through each checkbox
                    this.checked = true;  //select all checkboxes with class "checkbox85"               
                });
            }else{
                $('.checkbox85').each(function() { //loop through each checkbox
                    this.checked = false; //deselect all checkboxes with class "checkbox85"                       
                });         
            }
        });
    

            $("[id^=toggle]").hide();
            $(".270Check").change(function () {
                $("[id=toggle270]").toggle();
            });

        $('#selectall270').click(function(event) {  //on click 
            if(this.checked) { // check select status
                $('.checkbox270').each(function() { //loop through each checkbox
                    this.checked = true;  //select all checkboxes with class "checkbox270"               
                });
            }else{
                $('.checkbox270').each(function() { //loop through each checkbox
                    this.checked = false; //deselect all checkboxes with class "checkbox270"                       
                });         
            }
        });
    

            $("[id^=toggle]").hide();
            $(".251Check").change(function () {
                $("[id=toggle251]").toggle();
            });

        $('#selectall251').click(function(event) {  //on click 
            if(this.checked) { // check select status
                $('.checkbox251').each(function() { //loop through each checkbox
                    this.checked = true;  //select all checkboxes with class "checkbox251"               
                });
            }else{
                $('.checkbox251').each(function() { //loop through each checkbox
                    this.checked = false; //deselect all checkboxes with class "checkbox251"                       
                });         
            }
        });
    

            $("[id^=toggle]").hide();
            $(".84Check").change(function () {
                $("[id=toggle84]").toggle();
            });

        $('#selectall84').click(function(event) {  //on click 
            if(this.checked) { // check select status
                $('.checkbox84').each(function() { //loop through each checkbox
                    this.checked = true;  //select all checkboxes with class "checkbox84"               
                });
            }else{
                $('.checkbox84').each(function() { //loop through each checkbox
                    this.checked = false; //deselect all checkboxes with class "checkbox84"                       
                });         
            }
        });
    

            $("[id^=toggle]").hide();
            $(".271Check").change(function () {
                $("[id=toggle271]").toggle();
            });

        $('#selectall271').click(function(event) {  //on click 
            if(this.checked) { // check select status
                $('.checkbox271').each(function() { //loop through each checkbox
                    this.checked = true;  //select all checkboxes with class "checkbox271"               
                });
            }else{
                $('.checkbox271').each(function() { //loop through each checkbox
                    this.checked = false; //deselect all checkboxes with class "checkbox271"                       
                });         
            }
        });
    

            $("[id^=toggle]").hide();
            $(".252Check").change(function () {
                $("[id=toggle252]").toggle();
            });

        $('#selectall252').click(function(event) {  //on click 
            if(this.checked) { // check select status
                $('.checkbox252').each(function() { //loop through each checkbox
                    this.checked = true;  //select all checkboxes with class "checkbox252"               
                });
            }else{
                $('.checkbox252').each(function() { //loop through each checkbox
                    this.checked = false; //deselect all checkboxes with class "checkbox252"                       
                });         
            }
        });
    

            $("[id^=toggle]").hide();
            $(".253Check").change(function () {
                $("[id=toggle253]").toggle();
            });

        $('#selectall253').click(function(event) {  //on click 
            if(this.checked) { // check select status
                $('.checkbox253').each(function() { //loop through each checkbox
                    this.checked = true;  //select all checkboxes with class "checkbox253"               
                });
            }else{
                $('.checkbox253').each(function() { //loop through each checkbox
                    this.checked = false; //deselect all checkboxes with class "checkbox253"                       
                });         
            }
        });
    

            $("[id^=toggle]").hide();
            $(".254Check").change(function () {
                $("[id=toggle254]").toggle();
            });

        $('#selectall254').click(function(event) {  //on click 
            if(this.checked) { // check select status
                $('.checkbox254').each(function() { //loop through each checkbox
                    this.checked = true;  //select all checkboxes with class "checkbox254"               
                });
            }else{
                $('.checkbox254').each(function() { //loop through each checkbox
                    this.checked = false; //deselect all checkboxes with class "checkbox254"                       
                });         
            }
        });
    

            $("[id^=toggle]").hide();
            $(".272Check").change(function () {
                $("[id=toggle272]").toggle();
            });

        $('#selectall272').click(function(event) {  //on click 
            if(this.checked) { // check select status
                $('.checkbox272').each(function() { //loop through each checkbox
                    this.checked = true;  //select all checkboxes with class "checkbox272"               
                });
            }else{
                $('.checkbox272').each(function() { //loop through each checkbox
                    this.checked = false; //deselect all checkboxes with class "checkbox272"                       
                });         
            }
        });
    

            $("[id^=toggle]").hide();
            $(".255Check").change(function () {
                $("[id=toggle255]").toggle();
            });

        $('#selectall255').click(function(event) {  //on click 
            if(this.checked) { // check select status
                $('.checkbox255').each(function() { //loop through each checkbox
                    this.checked = true;  //select all checkboxes with class "checkbox255"               
                });
            }else{
                $('.checkbox255').each(function() { //loop through each checkbox
                    this.checked = false; //deselect all checkboxes with class "checkbox255"                       
                });         
            }
        });
    

            $("[id^=toggle]").hide();
            $(".273Check").change(function () {
                $("[id=toggle273]").toggle();
            });

        $('#selectall273').click(function(event) {  //on click 
            if(this.checked) { // check select status
                $('.checkbox273').each(function() { //loop through each checkbox
                    this.checked = true;  //select all checkboxes with class "checkbox273"               
                });
            }else{
                $('.checkbox273').each(function() { //loop through each checkbox
                    this.checked = false; //deselect all checkboxes with class "checkbox273"                       
                });         
            }
        });
    

            $("[id^=toggle]").hide();
            $(".257Check").change(function () {
                $("[id=toggle257]").toggle();
            });

        $('#selectall257').click(function(event) {  //on click 
            if(this.checked) { // check select status
                $('.checkbox257').each(function() { //loop through each checkbox
                    this.checked = true;  //select all checkboxes with class "checkbox257"               
                });
            }else{
                $('.checkbox257').each(function() { //loop through each checkbox
                    this.checked = false; //deselect all checkboxes with class "checkbox257"                       
                });         
            }
        });
    

            $("[id^=toggle]").hide();
            $(".259Check").change(function () {
                $("[id=toggle259]").toggle();
            });

        $('#selectall259').click(function(event) {  //on click 
            if(this.checked) { // check select status
                $('.checkbox259').each(function() { //loop through each checkbox
                    this.checked = true;  //select all checkboxes with class "checkbox259"               
                });
            }else{
                $('.checkbox259').each(function() { //loop through each checkbox
                    this.checked = false; //deselect all checkboxes with class "checkbox259"                       
                });         
            }
        });
    

            $("[id^=toggle]").hide();
            $(".258Check").change(function () {
                $("[id=toggle258]").toggle();
            });

        $('#selectall258').click(function(event) {  //on click 
            if(this.checked) { // check select status
                $('.checkbox258').each(function() { //loop through each checkbox
                    this.checked = true;  //select all checkboxes with class "checkbox258"               
                });
            }else{
                $('.checkbox258').each(function() { //loop through each checkbox
                    this.checked = false; //deselect all checkboxes with class "checkbox258"                       
                });         
            }
        });
    

            $("[id^=toggle]").hide();
            $(".274Check").change(function () {
                $("[id=toggle274]").toggle();
            });

        $('#selectall274').click(function(event) {  //on click 
            if(this.checked) { // check select status
                $('.checkbox274').each(function() { //loop through each checkbox
                    this.checked = true;  //select all checkboxes with class "checkbox274"               
                });
            }else{
                $('.checkbox274').each(function() { //loop through each checkbox
                    this.checked = false; //deselect all checkboxes with class "checkbox274"                       
                });         
            }
        });
    

            $("[id^=toggle]").hide();
            $(".260Check").change(function () {
                $("[id=toggle260]").toggle();
            });

        $('#selectall260').click(function(event) {  //on click 
            if(this.checked) { // check select status
                $('.checkbox260').each(function() { //loop through each checkbox
                    this.checked = true;  //select all checkboxes with class "checkbox260"               
                });
            }else{
                $('.checkbox260').each(function() { //loop through each checkbox
                    this.checked = false; //deselect all checkboxes with class "checkbox260"                       
                });         
            }
        });
    

            $("[id^=toggle]").hide();
            $(".261Check").change(function () {
                $("[id=toggle261]").toggle();
            });

        $('#selectall261').click(function(event) {  //on click 
            if(this.checked) { // check select status
                $('.checkbox261').each(function() { //loop through each checkbox
                    this.checked = true;  //select all checkboxes with class "checkbox261"               
                });
            }else{
                $('.checkbox261').each(function() { //loop through each checkbox
                    this.checked = false; //deselect all checkboxes with class "checkbox261"                       
                });         
            }
        });
    

            $("[id^=toggle]").hide();
            $(".262Check").change(function () {
                $("[id=toggle262]").toggle();
            });

        $('#selectall262').click(function(event) {  //on click 
            if(this.checked) { // check select status
                $('.checkbox262').each(function() { //loop through each checkbox
                    this.checked = true;  //select all checkboxes with class "checkbox262"               
                });
            }else{
                $('.checkbox262').each(function() { //loop through each checkbox
                    this.checked = false; //deselect all checkboxes with class "checkbox262"                       
                });         
            }
        });
    

            $("[id^=toggle]").hide();
            $(".263Check").change(function () {
                $("[id=toggle263]").toggle();
            });

        $('#selectall263').click(function(event) {  //on click 
            if(this.checked) { // check select status
                $('.checkbox263').each(function() { //loop through each checkbox
                    this.checked = true;  //select all checkboxes with class "checkbox263"               
                });
            }else{
                $('.checkbox263').each(function() { //loop through each checkbox
                    this.checked = false; //deselect all checkboxes with class "checkbox263"                       
                });         
            }
        });
    

            $("[id^=toggle]").hide();
            $(".264Check").change(function () {
                $("[id=toggle264]").toggle();
            });

        $('#selectall264').click(function(event) {  //on click 
            if(this.checked) { // check select status
                $('.checkbox264').each(function() { //loop through each checkbox
                    this.checked = true;  //select all checkboxes with class "checkbox264"               
                });
            }else{
                $('.checkbox264').each(function() { //loop through each checkbox
                    this.checked = false; //deselect all checkboxes with class "checkbox264"                       
                });         
            }
        });
    

            $("[id^=toggle]").hide();
            $(".265Check").change(function () {
                $("[id=toggle265]").toggle();
            });

        $('#selectall265').click(function(event) {  //on click 
            if(this.checked) { // check select status
                $('.checkbox265').each(function() { //loop through each checkbox
                    this.checked = true;  //select all checkboxes with class "checkbox265"               
                });
            }else{
                $('.checkbox265').each(function() { //loop through each checkbox
                    this.checked = false; //deselect all checkboxes with class "checkbox265"                       
                });         
            }
        });
    

            $("[id^=toggle]").hide();
            $(".97Check").change(function () {
                $("[id=toggle97]").toggle();
            });

        $('#selectall97').click(function(event) {  //on click 
            if(this.checked) { // check select status
                $('.checkbox97').each(function() { //loop through each checkbox
                    this.checked = true;  //select all checkboxes with class "checkbox97"               
                });
            }else{
                $('.checkbox97').each(function() { //loop through each checkbox
                    this.checked = false; //deselect all checkboxes with class "checkbox97"                       
                });         
            }
        });
    

            $("[id^=toggle]").hide();
            $(".266Check").change(function () {
                $("[id=toggle266]").toggle();
            });

        $('#selectall266').click(function(event) {  //on click 
            if(this.checked) { // check select status
                $('.checkbox266').each(function() { //loop through each checkbox
                    this.checked = true;  //select all checkboxes with class "checkbox266"               
                });
            }else{
                $('.checkbox266').each(function() { //loop through each checkbox
                    this.checked = false; //deselect all checkboxes with class "checkbox266"                       
                });         
            }
        });
    

            $("[id^=toggle]").hide();
            $(".267Check").change(function () {
                $("[id=toggle267]").toggle();
            });

        $('#selectall267').click(function(event) {  //on click 
            if(this.checked) { // check select status
                $('.checkbox267').each(function() { //loop through each checkbox
                    this.checked = true;  //select all checkboxes with class "checkbox267"               
                });
            }else{
                $('.checkbox267').each(function() { //loop through each checkbox
                    this.checked = false; //deselect all checkboxes with class "checkbox267"                       
                });         
            }
        });
    

            $("[id^=toggle]").hide();
            $(".103Check").change(function () {
                $("[id=toggle103]").toggle();
            });

        $('#selectall103').click(function(event) {  //on click 
            if(this.checked) { // check select status
                $('.checkbox103').each(function() { //loop through each checkbox
                    this.checked = true;  //select all checkboxes with class "checkbox103"               
                });
            }else{
                $('.checkbox103').each(function() { //loop through each checkbox
                    this.checked = false; //deselect all checkboxes with class "checkbox103"                       
                });         
            }
        });
    

            $("[id^=toggle]").hide();
            $(".268Check").change(function () {
                $("[id=toggle268]").toggle();
            });

        $('#selectall268').click(function(event) {  //on click 
            if(this.checked) { // check select status
                $('.checkbox268').each(function() { //loop through each checkbox
                    this.checked = true;  //select all checkboxes with class "checkbox268"               
                });
            }else{
                $('.checkbox268').each(function() { //loop through each checkbox
                    this.checked = false; //deselect all checkboxes with class "checkbox268"                       
                });         
            }
        });
    

            $("[id^=toggle]").hide();
            $(".275Check").change(function () {
                $("[id=toggle275]").toggle();
            });

        $('#selectall275').click(function(event) {  //on click 
            if(this.checked) { // check select status
                $('.checkbox275').each(function() { //loop through each checkbox
                    this.checked = true;  //select all checkboxes with class "checkbox275"               
                });
            }else{
                $('.checkbox275').each(function() { //loop through each checkbox
                    this.checked = false; //deselect all checkboxes with class "checkbox275"                       
                });         
            }
        });
    

            $("[id^=toggle]").hide();
            $(".104Check").change(function () {
                $("[id=toggle104]").toggle();
            });

        $('#selectall104').click(function(event) {  //on click 
            if(this.checked) { // check select status
                $('.checkbox104').each(function() { //loop through each checkbox
                    this.checked = true;  //select all checkboxes with class "checkbox104"               
                });
            }else{
                $('.checkbox104').each(function() { //loop through each checkbox
                    this.checked = false; //deselect all checkboxes with class "checkbox104"                       
                });         
            }
        });
    

                $('#userData').bootstrapValidator({
        feedbackIcons: {
            valid: 'glyphicon glyphicon-ok',
            invalid: 'glyphicon glyphicon-remove',
            validating: 'glyphicon glyphicon-refresh'   
        },
        live: 'enabled',
        message: 'This value is not valid',
        submitButtons: 'button[type="submit"]',
        trigger: null,

        fields: {
            emailAddress: {
                validators: {
                    notEmpty: {
                        message: 'A selection is required'
                    }
                }
            },
            choosePassword: {
                validators: {
                    notEmpty: {
                        message: 'A selection is required'
                    }
                }
            },
            confirmPassword: {
                validators: {
                    notEmpty: {
                        message: 'A selection is required'
                    }
                }
            },

        }
        })
        .on('error.field.bv', function(e, data) {
            // $(e.target)  --> The field element
            // data.bv      --> The BootstrapValidator instance
            // data.field   --> The field name
            // data.element --> The field element

            data.bv.disableSubmitButtons(false);
        })
        .on('success.form.bv', function(e) {
                    window.location.replace("success.html");         
                });

/*
        .on('success.field.bv', function(e, data) {
            // e, data parameters are the same as in error.field.bv event handler
            // Despite that the field is valid, by default, the submit button will be disabled if all the following conditions meet
            // - The submit button is clicked
            // - The form is invalid
            data.bv.disableSubmitButtons(false);
        });
*/
/*
        .on('error.field.bv', function(e, data) {
            if (data.bv.getSubmitButton()) {
                data.bv.disableSubmitButtons(false);
            }
        })
        .on('success.field.bv', function(e, data) {
            if (data.bv.getSubmitButton()) {
                data.bv.disableSubmitButtons(false);
            }
        });
*/


        });
